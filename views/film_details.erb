<h1><%= @film.title %></h1>

<img src="/images/<%= @film.image %>" alt="<%= @film.title %>">

<h2>SCREENING TIMES</h2>

<ul>
  <% @screening_list = @film.get_screenings %>
  <li>(All screenings - £<%= @film.price %>)</li>
  <li>    <br/>  </li>
  <% for screening in @screening_list %>
  <li> <%= screening['show_time'] %></li>
  <% end %>
</ul>

<a href="http://localhost:4567/films">back to full listings</a>
